# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# GKE A4X delta (strategic merge): nvcr pull secret + explicit model mount on the
# frontend; on the workers the GKE Multus RDMA annotations, arm64/GB200 node
# selection, the RDMA tolerations + resources, rack-level anti-affinity, and the
# GIB/driver hostPath mounts. The frontend/worker service-level mounts from
# ../_base are removed in patch-deploy.json.yaml; env edits also live there.
apiVersion: nvidia.com/v1alpha1
kind: DynamoGraphDeployment
metadata:
  name: q32b-1p1d
spec:
  services:
    Frontend:
      extraPodSpec:
        imagePullSecrets:
        - name: nvcr-pull-secret
        mainContainer:
          volumeMounts:
          - { name: shared-model-cache, mountPath: /shared-models }
        volumes:
        - { name: shared-model-cache, persistentVolumeClaim: { claimName: shared-model-cache } }
    VllmPrefillWorker:
      extraPodMetadata:
        annotations:
          networking.gke.io/default-interface: "eth0"
          networking.gke.io/interfaces: |
            [{"interfaceName":"eth0","network":"default"},
             {"interfaceName":"eth2","network":"rdma-0"},
             {"interfaceName":"eth3","network":"rdma-1"},
             {"interfaceName":"eth4","network":"rdma-2"},
             {"interfaceName":"eth5","network":"rdma-3"}]
      extraPodSpec:
        nodeSelector:
          kubernetes.io/arch: arm64
          nvidia.com/gpu.product: NVIDIA-GB200
        imagePullSecrets:
        - name: nvcr-pull-secret
        tolerations:
        - { key: nvidia.com/gpu,                       operator: Exists, effect: NoSchedule }
        - { key: kubernetes.io/arch,                   operator: Equal, value: arm64, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-0,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-1,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-2,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-3,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-0.IP, operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-1.IP, operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-2.IP, operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-3.IP, operator: Exists, effect: NoSchedule }
        affinity:
          podAntiAffinity:
            # Two-level: different host AND different GCE topology block (~rack).
            # GB200 NVL72 shares NVLink within a rack; without rack-level
            # separation NIXL would short-circuit via cuda_ipc/NVLink (~900 GB/s)
            # and mask the RoCE bandwidth under measurement.
            requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                - key: nvidia.com/dynamo-sub-component-type
                  operator: In
                  values: [prefill, decode]
              topologyKey: cloud.google.com/gce-topology-block
            - labelSelector:
                matchExpressions:
                - key: nvidia.com/dynamo-sub-component-type
                  operator: In
                  values: [prefill, decode]
              topologyKey: kubernetes.io/hostname
        mainContainer:
          # /opt/nvidia/nvda_nixl/lib64 must come first (set in env) so NIXL
          # telemetry can load libcore.so.1.3; gib + nvidia driver libs are
          # bind-mounted from the host.
          volumeMounts:
          - { name: gib,    mountPath: /usr/local/gib }
          - { name: nvidia, mountPath: /usr/local/nvidia }
          - { name: shared-model-cache, mountPath: /shared-models }
        volumes:
        - { name: gib,    hostPath: { path: /home/kubernetes/bin/gib } }
        - { name: nvidia, hostPath: { path: /home/kubernetes/bin/nvidia } }
        - { name: shared-model-cache, persistentVolumeClaim: { claimName: shared-model-cache } }
      resources:
        limits:
          custom:
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
        requests:
          custom:
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
    VllmDecodeWorker:
      extraPodMetadata:
        annotations:
          networking.gke.io/default-interface: "eth0"
          networking.gke.io/interfaces: |
            [{"interfaceName":"eth0","network":"default"},
             {"interfaceName":"eth2","network":"rdma-0"},
             {"interfaceName":"eth3","network":"rdma-1"},
             {"interfaceName":"eth4","network":"rdma-2"},
             {"interfaceName":"eth5","network":"rdma-3"}]
      extraPodSpec:
        nodeSelector:
          kubernetes.io/arch: arm64
          nvidia.com/gpu.product: NVIDIA-GB200
        imagePullSecrets:
        - name: nvcr-pull-secret
        tolerations:
        - { key: nvidia.com/gpu,                       operator: Exists, effect: NoSchedule }
        - { key: kubernetes.io/arch,                   operator: Equal, value: arm64, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-0,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-1,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-2,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-3,    operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-0.IP, operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-1.IP, operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-2.IP, operator: Exists, effect: NoSchedule }
        - { key: networking.gke.io.networks/rdma-3.IP, operator: Exists, effect: NoSchedule }
        affinity:
          podAntiAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                - key: nvidia.com/dynamo-sub-component-type
                  operator: In
                  values: [prefill, decode]
              topologyKey: cloud.google.com/gce-topology-block
            - labelSelector:
                matchExpressions:
                - key: nvidia.com/dynamo-sub-component-type
                  operator: In
                  values: [prefill, decode]
              topologyKey: kubernetes.io/hostname
        mainContainer:
          volumeMounts:
          - { name: gib,    mountPath: /usr/local/gib }
          - { name: nvidia, mountPath: /usr/local/nvidia }
          - { name: shared-model-cache, mountPath: /shared-models }
        volumes:
        - { name: gib,    hostPath: { path: /home/kubernetes/bin/gib } }
        - { name: nvidia, hostPath: { path: /home/kubernetes/bin/nvidia } }
        - { name: shared-model-cache, persistentVolumeClaim: { claimName: shared-model-cache } }
      resources:
        limits:
          custom:
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
        requests:
          custom:
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
